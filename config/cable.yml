redis: &redis
  adapter: redis
  url: redis://localhost:6379/1

development:
  <<: *redis

test:
  <<: *redis

production:
  <<: *redis
  url: <%= ENV["REDIS_URL"] %>
  channel_prefix: drluca_production
